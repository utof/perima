/**
 * Index route placeholder — Task 7 replaces with the IndexRoute body
 * (file/tag/search composition extracted from App.tsx).
 *
 * WHY placeholder commit: Router setup (Task 6) is independent from
 * content extraction (Task 7). Splitting them keeps each commit
 * reviewable.
 */
export default function IndexRoute() {
  return (
    <main className="flex-1 overflow-auto p-4">
      <p className="text-gray-300">Loading…</p>
    </main>
  );
}
