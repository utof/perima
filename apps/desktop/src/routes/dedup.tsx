/**
 * /dedup route — placeholder stub.
 *
 * WHY stub: the full dedup management UI (Task 13) lands in the next task.
 * The route must exist here so `<Link to="/dedup">` in CollisionPill (Task 12)
 * passes TanStack Router's compile-time route validation.
 *
 * Task 13 will replace this body with the collision-group table, verify
 * actions, and status filtering.
 */
export default function DedupRoute() {
  return (
    <div className="flex-1 p-8 text-gray-400">
      <p>Dedup management coming soon.</p>
    </div>
  );
}
